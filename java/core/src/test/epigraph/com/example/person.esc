namespace com.example

vartype Person default id {
  id: PersonId
  `record`: PersonRecord
}

integer PersonId

record PersonRecord {
  id: PersonId
  bestFriend: Person//Record
  friends: list[PersonRecord]
  ids: list[PersonId]
  idss: list[list[PersonId]]
}
