namespace com.example

vartype Person {
  id: PersonId
  `record`: PersonRecord
}

integer PersonId

record PersonRecord {
  id:  PersonId
  firstName: String
  bestFriend:  Person retro id//Record
  friends: list[PersonRecord]
  friendsMap: map[PersonId, Person retro id]
  ids: list[PersonId]
  idss: list[list[PersonId]]
  persons: list[Person]
  personss: list[list[Person]]
  strings: map[String, String]
  mapppsss: map[map[list[String], list[Integer]], map[UserId, map[list[UserId], User retro id]]]
}

list[Person] PersonList

map[String, Person] PersonsMap

map[String, Integer] StrToInt

vartype Bad {
  //bad: Person
}

//list[Self] Self
