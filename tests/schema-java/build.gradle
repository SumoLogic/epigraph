plugins {
  id 'ws.epigraph.java' version "0.0"
}

group = 'ws.epigraph.tests'

dependencies {
  epigraphSchema project(':tests-schema')

  compile project(':epigraph-java-core')

  testCompile 'junit:junit'
}
