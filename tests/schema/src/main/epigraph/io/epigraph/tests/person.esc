namespace io.epigraph.tests

vartype Person {
  id: PersonId
  `record`: PersonRecord
}

integer PersonId

record PersonRecord {
  avroType = "Person"
  dbTable = "T_PERSONS"
  id: PersonId { avroField = "personId", dbColumn = "ID", dbType = "BIGINT" }
  firstName: String
  lastName: String
  bestFriend: Person // default id
  worstEnemy: PersonRecord
  friends: list[Person]
  friendsMap: map[String, Person]
}

map[PersonId,Person] UsersResourceDummy
