namespace some

import foo

record R {
  // todo fix spacing between annotation and value
  @Attr #value
  @Attr1 #value
  @Attr2 (#a: #b, #c: <d: #e> #z: #x)
  f: map[Q, list[W]]
}

list[list[Bar]] bars

abstract record E

enum[String] E {
  @A "foo" B: "bar"
  C: "baz"
}
