namespace epigraph

string String {
  // TODO add annotations altering generated data and type names (to not collide with native String)
}

integer Integer

long Long

double Double

boolean Boolean

vartype Foo {
  `string`: Long
}

vartype Bar extends Foo {
  override `string`: Double
}